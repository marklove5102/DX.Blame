---
phase: 01-package-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/DX.Blame.Tests.dproj
  - tests/DX.Blame.Tests.dpr
  - tests/DX.Blame.Tests.Version.pas
  - DX.Blame.groupproj
autonomous: false
requirements:
  - UX-04

must_haves:
  truths:
    - "DUnitX tests for version constants pass"
    - "BPL installs in Delphi IDE via Component > Install Packages"
    - "Plugin appears in IDE splash screen"
    - "Plugin appears in Help > About dialog"
    - "DX Blame submenu appears under Tools with two disabled items"
    - "BPL uninstalls without crashes or access violations"
  artifacts:
    - path: "tests/DX.Blame.Tests.dproj"
      provides: "DUnitX test project"
    - path: "tests/DX.Blame.Tests.dpr"
      provides: "DUnitX test runner program"
    - path: "tests/DX.Blame.Tests.Version.pas"
      provides: "Unit tests for version constants"
      min_lines: 20
  key_links:
    - from: "tests/DX.Blame.Tests.Version.pas"
      to: "src/DX.Blame.Version.pas"
      via: "uses clause"
      pattern: "uses.*DX\\.Blame\\.Version"
    - from: "DX.Blame.groupproj"
      to: "tests/DX.Blame.Tests.dproj"
      via: "project group entry"
      pattern: "DX\\.Blame\\.Tests"
---

<objective>
Add DUnitX test project with version constant tests, update the project group, and verify the BPL installs and unloads correctly in the Delphi IDE.

Purpose: Validate that the package foundation works end-to-end -- from compilation through IDE installation, registration visibility, and clean unload.
Output: Working test suite + confirmed IDE integration.
</objective>

<execution_context>
@C:/Users/Test/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-package-foundation/01-CONTEXT.md
@.planning/phases/01-package-foundation/01-RESEARCH.md
@.planning/phases/01-package-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 output -- executor needs these contracts -->

From src/DX.Blame.Version.pas:
```pascal
const
  cDXBlameMajorVersion = 1;
  cDXBlameMinorVersion = 0;
  cDXBlameRelease = 0;
  cDXBlameBuild = 0;
  cDXBlameVersion = '1.0.0.0';
  cDXBlameName = 'DX.Blame';
  cDXBlameDescription = 'Git Blame for Delphi';
  cDXBlameCopyright = 'Copyright (c) 2026 Olaf Monien';
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DUnitX test project with version constant tests</name>
  <files>
    libs/DUnitX (git submodule),
    tests/DX.Blame.Tests.dpr,
    tests/DX.Blame.Tests.dproj,
    tests/DX.Blame.Tests.Version.pas,
    DX.Blame.groupproj
  </files>
  <behavior>
    - Test: cDXBlameVersion equals '1.0.0.0'
    - Test: cDXBlameVersion matches format 'N.N.N.N' (four dot-separated numbers)
    - Test: cDXBlameName equals 'DX.Blame'
    - Test: cDXBlameDescription is not empty
    - Test: cDXBlameCopyright contains 'Olaf Monien'
    - Test: cDXBlameMajorVersion, cDXBlameMinorVersion, cDXBlameRelease, cDXBlameBuild are non-negative integers
    - Test: Assembled version string from Major.Minor.Release.Build matches cDXBlameVersion
  </behavior>
  <action>
    1. Add DUnitX as git submodule: `git submodule add https://github.com/VSoftTechnologies/DUnitX.git libs/DUnitX`

    2. Create `tests/DX.Blame.Tests.dpr` -- DUnitX console test runner program. Uses DUnitX.TestFramework, DUnitX.Loggers.Console, DX.Blame.Tests.Version. Standard DUnitX runner boilerplate with TDUnitX.RegisterTestFixture and runner.Execute.

    3. Create `tests/DX.Blame.Tests.dproj` -- Delphi console application project.
       - Output path: ../build/$(Platform)/$(Config)
       - DCU output path: ../build/$(Platform)/$(Config)/dcu
       - Search path must include: ../src;../libs/DUnitX/Source;../libs/DUnitX/Source/Loggers;../libs/DUnitX/Source/Loggers/Console
       - Target: Win64 console application
       - IncludeVerInfo=True, version 1.0.0.0, CompanyName=Olaf Monien

    4. Create `tests/DX.Blame.Tests.Version.pas` with XML doc header (MIT license). Test fixture class TVersionTests with [TestFixture] attribute. Implement all tests from behavior section using Assert.AreEqual, Assert.IsNotEmpty, Assert.Contains, Assert.IsTrue.

    5. Update `DX.Blame.groupproj` to include tests/DX.Blame.Tests.dproj as second project in the group.

    All .pas/.dpr files: UTF-8 with BOM, CRLF line endings. Follow CLAUDE.md naming conventions.
  </action>
  <verify>
    <automated>powershell -File build/DelphiBuildDPROJ.ps1 tests/DX.Blame.Tests.dproj</automated>
  </verify>
  <done>DUnitX submodule cloned. Test project compiles. Version tests validate all constants from DX.Blame.Version. Project group contains both package and test projects.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify BPL installation and IDE integration</name>
  <files>N/A -- verification only, no files modified</files>
  <action>
    Present the user with the verification checklist below. The BPL was built in Plan 01 and tests were created in Task 1. This checkpoint confirms the full IDE integration works end-to-end. No code changes needed -- purely manual verification in the Delphi IDE.
  </action>
  <verify>User confirms all 10 verification steps pass in the IDE</verify>
  <done>User has approved that BPL installs, registers (splash, about, menu), and unloads cleanly in Delphi 13</done>
  <what-built>Complete DX.Blame design-time package with OTA registration (splash screen, About dialog, Tools menu placeholder) and clean unload support.</what-built>
  <how-to-verify>
    1. Open Delphi 13 (primary target)
    2. Watch the splash screen during IDE startup -- DX.Blame should NOT appear yet (not installed)
    3. Go to Component > Install Packages > Add
    4. Navigate to build/Win64/Release/ (or Win32/Debug/ depending on build) and select DX.Blame.bpl
    5. Confirm "DX.Blame" appears in the installed packages list with description "DX.Blame - Git Blame for Delphi"
    6. Go to Help > About -- verify "DX.Blame" appears in the plugin list with "Git Blame for Delphi" description and version "1.0.0.0"
    7. Go to Tools menu -- verify "DX Blame" submenu exists with "Enable Blame" (greyed out) and "Settings..." (greyed out)
    8. Go back to Component > Install Packages, select DX.Blame, click Remove
    9. Verify no crash or access violation occurs
    10. Verify "DX Blame" is gone from the Tools menu
    11. Close and reopen the IDE -- verify no residual errors
    12. Optionally: reinstall the BPL and restart the IDE to verify splash screen shows "DX.Blame" on startup
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
1. DUnitX submodule exists at libs/DUnitX/
2. Test project compiles: `powershell -File build/DelphiBuildDPROJ.ps1 tests/DX.Blame.Tests.dproj`
3. Tests pass: `build/Win64/Debug/DX.Blame.Tests.exe` exits with code 0
4. BPL installs in Delphi IDE without errors
5. Plugin visible in splash, about, and tools menu
6. BPL uninstalls without crashes
</verification>

<success_criteria>
- DUnitX test project compiles and all version tests pass
- User confirms BPL installs, shows in splash/about/menu, and unloads cleanly in Delphi 13
- No access violations or memory leaks on install/uninstall cycle
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-foundation/01-02-SUMMARY.md`
</output>
